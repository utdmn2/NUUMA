  <% posts.last(4).each do |post| %>
  <div class="col-sm mt-2">
  <div class="card"　style="width: 18rem;">
    <%= link_to post_path(post.id) do %>
    <%= attachment_image_tag(post, :image, :fill, 100, 60, class: 'card-img-top rounded', fallback: "no_image.jpg" ) %>
    <% end %>
  <div class="card-body">
    <h5 class="card-title"><%= post.title %></h5>
      <p class="card-text"><%= truncate(post.body) %></p>
        <div class="profile_image">
          <%= link_to user_path(post.user) do %>
            <%= attachment_image_tag post.user, :profile_image, :fill, 30, 30 %>
            <%= post.user.name %>
          <% end %>
        </div>
      <p class="card-text text-right"><small class="text-muted"><%= post.created_at.strftime('%Y/%m/%d') %></small></p>
    </div>
  </div>
  </div>
<% end %>