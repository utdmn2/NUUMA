<h3>新着投稿一覧</h3>
<% @posts.each do |post| %>
<%= attachment_image_tag post, :image, :fill, 30, 20 %>
<%= link_to post.title, post_path(post.id) %>
<%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %>
<% end %>

