<!DOCTYPE html>
<html>
  <head>
    <title>Nuuma</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/35d3da8b06.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:wght@300&display=swap" rel="stylesheet">
  </head>

  <body>
    <div id="header" class="sticky-top">
      <nav class="navbar navbar-expand-lg text-dark" >
        <div class="container">
          <%= link_to image_tag("logo.png", size: "140x100"), root_path %>
          <div class="navbar navbar-dark">
            <div class="container d-flex justify-content-between">
              <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
            </div>
          </div>
              <!--管理者側ログイン後メニュー-->
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

            <% if admin_signed_in? %>
              <li class="nav-item">
                <%= link_to admin_users_path, class: "nav-link text-dark" do %>
                会員一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to admin_posts_path, class: "nav-link text-dark" do %>
                投稿一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to admin_categories_path, class: "nav-link text-dark" do %>
                カテゴリ一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to destroy_admin_session_path, class: "nav-link text-dark", method: :delete do %>
                ログアウト
                <% end %>
              </li>

              <!--会員側ログイン後メニュー-->
            <% elsif user_signed_in? %>
              <li class="nav-item">
                <%= link_to user_path(current_user.id), class: "nav-link text-dark" do %>
                マイページ
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_post_path, class: "nav-link text-dark" do %>
                投稿する
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to posts_path, class: "nav-link text-dark" do %>
                投稿一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to destroy_user_session_path, class: "nav-link text-dark", method: :delete do %>
                ログアウト
                <% end %>
              </li>

              <!-- 会員側ログイン前メニュー -->
            <% else %>
              <li class="nav-item">
                <%= link_to posts_path, class: "nav-link text-dark" do %>
                投稿一覧
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: "nav-link text-dark" do %>
                新規会員登録
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "nav-link text-dark" do %>
                ログイン
                <% end %>
              </li>
            <% end %>

            <!-- 検索窓　-->
            </ul>
            <form class="d-flex">
              <%= form_with url:'/searches/search', method: :get, local: true do |f| %>
            　  <%= f.select 'range' ,options_for_select([ ['ユーザー', '1'], ['投稿', '2']]) %>
            　  <%= f.select 'search', options_for_select([["前方一致","forward_match"], ["後方一致","backward_match"], ["完全一致","perfect_match"], ["部分一致","partial_match"]]) %>
            　  <%= f.text_field :word %>
            　  <%= f.submit '検索', class: 'btn btn-outline-dark ml-2' %>
            　<% end %>
            </form>
           </div>
         </div>
      </nav>
    </div>



    <div class="container">
      <%= yield %>
    </div>


    <!-- フッター -->
    <footer class="footer">
      <div class="container">
        <div class="row">
          <div class="mx-auto">
            <p class="pt-3" style="font-size:15px;">
              <%= link_to "投稿ルール", post_rule_path, class: "text-dark" %>
              |
              <%= link_to "利用規約", guideline_path, class: "text-dark" %>
              |
              <%= link_to "プライバシーポリシー", privacy_path, class: "text-dark" %>
            </p>
            <p style="font-size:12px;">Copyright ©NUUMA 2021 All Rights Reserved.</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
